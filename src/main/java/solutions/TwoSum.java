package solutions;
//Problem 01.Two Sum:
//Solution in C

/*
int* twoSum(int* nums, int numsSize, int target) {
        int i;
        int* return_arr;
        int temp_index=0;

        return_arr = (int*)malloc(sizeof(return_arr)*2);

        while(temp_index<numsSize){
        for(i=0; i<numsSize; i++){
        if(nums[i]+nums[temp_index]==target && temp_index!=i){
        return_arr[1] = temp_index;
        return_arr[0] = i;
        }
        }
        temp_index++;
        }

        return return_arr;
        }
*/
